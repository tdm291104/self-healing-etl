# =============================================================
# Self-Healing ETL Pipeline — Environment Variables
# Copy this to .env and fill in your values.
# Generate AIRFLOW_FERNET_KEY with:
#   python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# =============================================================

# Airflow
AIRFLOW_FERNET_KEY=46BKJoQYlPPOexq0OhDZnIlNepKFf28zYzoJe3y4d-U=
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=admin

# PostgreSQL superuser (used to bootstrap databases)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Airflow metadata DB credentials
AIRFLOW_DB_USER=airflow
AIRFLOW_DB_PASSWORD=airflow
AIRFLOW_DB_NAME=airflow_db

# Data warehouse credentials (used by dbt and Airflow DAGs)
DW_DB_USER=dbt_user
DW_DB_PASSWORD=dbt_password
DW_DB_NAME=crypto_dw

# Telegram Bot (Phase 3 — leave empty for now)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Claude API (Phase 3)
ANTHROPIC_API_KEY=
